# RssReader
RssReader with some options.

# Requirements
1) JRE/JDK 1.8+
2) Apache Maven 3.0
3) Internet access

# Creating jar file:
terminal ->
"mvn clean test compile assembly:single"

# Running the app:
terminal ->
"java -jar <jar-name>"
